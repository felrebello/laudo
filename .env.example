# Supabase Configuration (Optional - App works in offline mode without these)
# Get these from your Supabase project dashboard: https://app.supabase.com

VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# If these variables are not set, the app will run in offline mode using localStorage
# In offline mode, data persists only in the browser's localStorage
